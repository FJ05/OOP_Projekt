#include "assets.h"

//constructor definition
Division::Division(int from, int to, std::string n, std::string d, std::string opt)
    : ageFrom(from),
      ageTo(to),
      name(n),
      desc(d),
      optDesc(opt)
{
}